# Ralph Progress Log
Started: Sun Jan 25 16:15:30 CST 2026

## Codebase Patterns
- Use `./venv/bin/pip3.13` for installing deps (venv has mixed python 3.13/3.14)
- Downloader clients follow same pattern as `QbDownloader` in `backend/src/module/downloader/client/qb_downloader.py`
- Factory pattern in `download_client.py` with `__getClient()` static method
- pikpakapi is async library, requires `_run_async()` bridge pattern for sync interface

---

## 2026-01-25 - US-001
- What was implemented: Created PikPakDownloader class skeleton with async/sync bridge pattern
- Files changed:
  - `backend/src/module/downloader/client/pikpak_downloader.py` (new)
  - `backend/requirements.txt` (added pikpakapi==0.1.11)
- **Learnings for future iterations:**
  - The venv has both python3.13 and python3.14; use `./venv/bin/pip3.13` for installs
  - pikpakapi library version is 0.1.11 (not 0.0.34 from old docs)
  - Use `asyncio.new_event_loop()` pattern for sync wrapper to avoid nested loop issues
  - The `CLOUD_FOLDER = "AutoBangumi"` constant is module-level, not class attribute
---

## 2026-01-25 - US-002
- What was implemented: Integrated PikPakDownloader into the downloader factory
- Files changed:
  - `backend/src/module/downloader/download_client.py` (added pikpak case in __getClient())
  - `backend/src/module/downloader/client/__init__.py` (added exports for both downloaders)
- **Learnings for future iterations:**
  - The factory pattern uses local imports inside the conditional branches to avoid circular imports
  - PikPak only needs username/password (ignores host/ssl that qBittorrent uses)
  - The `__init__.py` was previously empty - now exports both PikPakDownloader and QbDownloader
---

## 2026-01-25 - US-003
- What was implemented: PikPak authentication with token persistence
- Files changed:
  - `backend/src/module/downloader/client/pikpak_downloader.py` (added auth, logout, check_host, token helpers)
- **Learnings for future iterations:**
  - Token file at `/config/pikpak_token.json` - JSON format with access_token, refresh_token, user_id, expires_at
  - Access token expires in 7200 seconds (2 hours) - use 5-min buffer when checking validity
  - pikpakapi has `encode_token()` to update internal encoded_token after setting tokens manually
  - check_host() should try token refresh first (faster), then fall back to full login
  - Use `os.makedirs(os.path.dirname(TOKEN_FILE), exist_ok=True)` to ensure /config dir exists
---

## 2026-01-25 - US-004
- What was implemented: Torrent download functionality via PikPak
- Files changed:
  - `backend/src/module/downloader/client/pikpak_downloader.py` (added add_torrents, _get_or_create_folder, _extract_hash)
- **Learnings for future iterations:**
  - pikpakapi has `path_to_id(path, create=True)` that handles nested folder creation recursively - perfect for _get_or_create_folder
  - `offline_download(file_url, parent_id)` is the method to add magnet URLs for download
  - Magnet hash pattern: `urn:btih:([a-fA-F0-9]{40})` - 40 hex characters, case-insensitive
  - The `_hash_map` dictionary tracks torrent_hash -> cloud_path for tracking downloads
  - PikPak uses folders instead of categories; the category parameter is ignored
---

## 2026-01-25 - US-005
- What was implemented: Torrent status retrieval and path lookup
- Files changed:
  - `backend/src/module/downloader/client/pikpak_downloader.py` (added torrents_info, get_torrent_path, TorrentInfo dataclass, PHASE_STATE_MAP)
- **Learnings for future iterations:**
  - pikpakapi `offline_list(phase=["PHASE_TYPE_RUNNING",...])` accepts a list of phase filters
  - PikPak phases: PHASE_TYPE_PENDING, PHASE_TYPE_RUNNING, PHASE_TYPE_COMPLETE, PHASE_TYPE_ERROR
  - Map to qBittorrent states: stalledDL, downloading, completed, error (for UI compatibility)
  - Created TorrentInfo dataclass to provide attribute access like qBittorrent's TorrentDictionary
  - Progress from PikPak is 0-100 integer, convert to 0.0-1.0 float for compatibility
  - get_torrent_path falls back to searching offline tasks if hash not in _hash_map
---

## 2026-01-25 - US-006
- What was implemented: File renaming via PikPak API
- Files changed:
  - `backend/src/module/downloader/client/pikpak_downloader.py` (added torrents_rename_file, _find_file_id_by_path)
- **Learnings for future iterations:**
  - pikpakapi's `file_rename(id, new_file_name)` requires file ID, not path
  - Use `path_to_id(path, create=False)` to look up file ID from full cloud path
  - PikPak API only accepts filename (not full path) in file_rename - use `os.path.basename()`
  - The renamer passes `old_path` and `new_path` relative to torrent folder, must combine with base_path from get_torrent_path()
  - Follow QbDownloader pattern: return True on success, False on file not found or error
---

## 2026-01-25 - US-007
- What was implemented: File move via PikPak API
- Files changed:
  - `backend/src/module/downloader/client/pikpak_downloader.py` (added move_torrent method)
- **Learnings for future iterations:**
  - pikpakapi has `file_batch_move(ids, to_parent_id)` for moving multiple files at once
  - Also available: `file_move_or_copy_by_path(from_path, to_path, move=True, create=True)` for path-based moves
  - QbDownloader's `move_torrent(hashes, new_location)` maps to `file_batch_move` in PikPak
  - Must collect file IDs first using torrent hash -> torrents_info() -> task name -> path_to_id
  - Update `_hash_map` after move to reflect new location for future operations
---

## 2026-01-25 - US-008
- What was implemented: Torrent deletion via PikPak API
- Files changed:
  - `backend/src/module/downloader/client/pikpak_downloader.py` (added torrents_delete method)
- **Learnings for future iterations:**
  - pikpakapi has `delete_tasks(task_ids, delete_files=True)` for deleting offline download tasks
  - For completed files, use `delete_to_trash(ids)` - files go to PikPak trash, not permanent delete
  - `delete_forever(ids)` exists but is dangerous - avoid using it
  - The method is idempotent - returns silently if torrent/file not found
  - Task IDs come from `offline_list()` response, file IDs from `path_to_id()`
  - Always clean up `_hash_map` when deleting a torrent to avoid stale entries
---

## 2026-01-25 - US-009
- What was implemented: Hash tracking method for deduplication
- Files changed:
  - `backend/src/module/downloader/client/pikpak_downloader.py` (added get_existing_hashes method)
- **Learnings for future iterations:**
  - `get_existing_hashes(category=None)` returns set of lowercase 40-char hex hashes
  - The category parameter is ignored (PikPak uses folders not categories) but kept for API compatibility
  - Reuses the same `offline_list()` call pattern from `torrents_info()` for consistency
  - Uses `_extract_hash()` to parse magnet URLs - same helper used in `add_torrents()` and `torrents_info()`
  - Returns empty set on error to match QbDownloader's graceful degradation pattern
---

## 2026-01-25 - US-010
- What was implemented: Stub methods for API compatibility with QbDownloader
- Files changed:
  - `backend/src/module/downloader/client/pikpak_downloader.py` (added 11 stub methods)
- **Learnings for future iterations:**
  - All stub methods have detailed docstrings explaining WHY they're stubs (PikPak doesn't support these features)
  - `add_category()` and `set_category()` log debug messages instead of being completely silent (helpful for debugging)
  - Methods that return data (`rss_get_feeds()`, `get_download_rule()`) return empty dict `{}` not `None`
  - The stub section is clearly marked with a comment header for easy identification
---

## 2026-01-25 - US-011
- What was implemented: Integration test configuration with PikPak documentation
- Files changed:
  - `docs/config/downloader.md` (added PikPak downloader section with config example and rclone setup)
  - `dev-config/backend/config_dev.json` (local-only, gitignored - added PikPak config example)
- **Learnings for future iterations:**
  - `dev-config/` is gitignored - use `docs/config/downloader.md` for tracked documentation
  - JSON config files can use `_comment` fields as a convention for inline documentation
  - rclone is required for PikPak to expose cloud files to local media servers (Jellyfin/Plex)
  - rclone mount command: `rclone mount pikpak: /mnt/pikpak --vfs-cache-mode full --daemon`
  - PikPak only needs `type`, `username`, `password`, `path` - ignores `host` and `ssl` params
---
